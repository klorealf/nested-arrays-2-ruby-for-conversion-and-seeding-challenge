require_relative '../convert_table'

describe 'converting a roster from a nested array to a collection of hashes' do
  let (:board) {convert_table}

    it 'takes a nested array representing a data table' do
    end
    it 'transforms each data row into a hash' do
    end
    it 'use the table header row data as keys' do
    end
end
